public class Game {
    private GameField gameField;
    public void fillPossibleeMoves(int player1, int player2) {
        for (int i = 0; i < 8; i++) {
            for (int j = 0; j < 8; j++) {

            }
        }
    }
    private boolean posExists(int x, int y) {
        return (x >= 0 && x < 8 && y >= 0 && y < 8);
    }
}
